name: cd

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.0"

      - name: Build
        run: ./scripts/buildprod.sh

      - id: auth
        name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Verify gcloud
        run: gcloud info

      - name: Build Docker Image
        run: gcloud builds submit --tag us-central1-docker.pkg.dev/project-a7d7ff50-c2ee-4dc8-aaf/notely-ar-repo/notely .

      - name: Deploy to Cloud Run
        run: gcloud run deploy notely --image us-central1-docker.pkg.dev/project-a7d7ff50-c2ee-4dc8-aaf/notely-ar-repo/notely:latest --region us-central1 --allow-unauthenticated --project project-a7d7ff50-c2ee-4dc8-aaf --max-instances=4